# 柜台特性

## 基本信息

`柜台`：xtp

`支持环境`：实盘、测试

`支持品种`：股票

`支持下单形式`：普通下单

## 特殊功能

`多点登录`：允许同一账户在不同机器上登录，并且可用 ID 区分不同机器。

`测试环境行情流量控制`：允许关闭自动获取可交易标的设置以达到限制流量的效果。本设置（查询 instruments）默认关闭，关闭时，将不会自动请求可交易标的（白名单），防止测试账号因流量过大被锁定。
